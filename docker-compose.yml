services:
  ttv-category-notifier:
    build: .
    container_name: ttv-category-notifier
    restart: unless-stopped
    env_file: .env
    environment:
      # Default state path is /data/state.json; can override here if desired
      - STATE_FILE=/data/state.json
    volumes:
      - ./data:/data
